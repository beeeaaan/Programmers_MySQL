SELECT A.CATEGORY, SUM(SALES) AS TOTAL_SALES  # 카테고리, 총 판매량
FROM BOOK A
INNER JOIN BOOK_SALES B ON A.BOOK_ID = B.BOOK_ID
WHERE B.SALES_DATE LIKE '2022-01%'  # 2022년 1월
GROUP BY A.CATEGORY  # 카테고리별
ORDER BY A.CATEGORY;  # 오름차순 정렬
