SELECT USER_ID, PRODUCT_ID
FROM ONLINE_SALE
GROUP BY USER_ID, PRODUCT_ID
HAVING COUNT (*) >= 2  // 2번 이상이면 재구매
ORDER BY USER_ID, PRODUCT_ID DESC  // 회원 ID 같을경우 상품 ID 내림차순

// SELECT 속성
// FROM 테이블
// GROUP 속성
// HAVING 조건절
// ORDER 속성 (ASC : 오름차순 / DESC : 내림차순)
