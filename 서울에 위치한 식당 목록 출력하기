SELECT R.REST_ID, I.REST_NAME, I.FOOD_TYPE, I.FAVORITES, I.ADDRESS, ROUND(AVG(R.REVIEW_SCORE),2) AS SCORE  #소수점 세 번째 자리 반올림하고 평균 점수 기준
FROM REST_REVIEW R
JOIN REST_INFO I ON R.REST_ID = I.REST_ID
GROUP BY R.REST_ID
HAVING I.ADDRESS LIKE '서울%'  # 서울에 위치
ORDER BY SCORE DESC, I.FAVORITES DESC;
