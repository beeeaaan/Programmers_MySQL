SELECT A.ID, A.GENOTYPE, B.GENOTYPE AS PARENT_GENOTYPE
FROM ECOLI_DATA A, ECOLI_DATA B
WHERE A.PARENT_ID = B.ID  // PARENT_ID = ID로 조인
AND B.GENOTYPE & A.GENOTYPE = B.GENOTYPE  // 부모 형질의 2진수와 같으면 부모 형질을 갖고 있는 것
ORDER BY ID
