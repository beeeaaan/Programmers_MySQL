SELECT COUNT(*) AS FISH_COUNT, MAX(IFNULL(LENGTH, 10)) AS MAX_LENGTH, FISH_TYPE  # 10cm이하(NULL) 물고기 10cm로 취급
FROM FISH_INFO
GROUP BY FISH_TYPE
HAVING AVG(IFNULL(LENGTH, 10)) >= 33  # 평균 길이 33cm 이상
ORDER BY FISH_TYPE
