SELECT A.ID , FISH_NAME, A.LENGTH 
FROM FISH_INFO A JOIN FISH_NAME_INFO B ON A.FISH_TYPE = B.FISH_TYPE
WHERE A.FISH_TYPE IN (
    SELECT FISH_TYPE FROM FISH_INFO
    GROUP BY FISH_TYPE  # 종류별
    HAVING LENGTH = MAX(LENGTH)  # 가장 큰 물고기
)
ORDER BY ID
