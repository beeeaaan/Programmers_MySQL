SELECT INS.ANIMAL_ID, INS.NAME
FROM ANIMAL_INS INS
INNER JOIN ANIMAL_OUTS OUTS
ON INS.ANIMAL_ID = OUTS.ANIMAL_ID  // 보호 테이블, 입양 테이블 조인
WHERE INS.DATETIME > OUTS.DATETIME  // 보호시작일보다 입양일이 더 빠를 경우
ORDER BY INS.DATETIME;
