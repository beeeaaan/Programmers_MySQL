SELECT YEAR(YM) AS YEAR, ROUND(AVG(PM_VAL1),2) AS PM10, ROUND(AVG(PM_VAL2),2) AS 'PM2.5'  // 평균 미세먼지 오염도와 평균 초미세먼지 오염도를 소수점 세 번째 자리에서 반올림
FROM AIR_POLLUTION
GROUP BY YEAR, LOCATION1, LOCATION2  // 연도 별
HAVING LOCATION2 = '수원'  // 수원 지역
ORDER BY 1;
