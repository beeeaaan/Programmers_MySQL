SELECT DISTINCT(C.CAR_ID)  // 중복 없이
FROM CAR_RENTAL_COMPANY_CAR AS C
INNER JOIN CAR_RENTAL_COMPANY_RENTAL_HISTORY AS H ON C.CAR_ID = H.CAR_ID
WHERE CAR_TYPE = '세단' AND MONTH(START_DATE) = 10  // '세단'인 자동차 중 10월에 대여
ORDER BY CAR_ID DESC;  // 내림차순 정렬

// DISTINCT : 중복 제거
SELECT DISTINCT(속성)
FROM 테이블
