SELECT DISTINCT D.ID, D.EMAIL, D.FIRST_NAME, D.LAST_NAME
FROM DEVELOPERS D JOIN SKILLCODES S ON S.CODE = D.SKILL_CODE & S.CODE  -- & 연산
WHERE S.CATEGORY LIKE 'Front End'  -- Front End 스킬
ORDER BY D.ID;
