SELECT A.FLAVOR
FROM FIRST_HALF A JOIN(  -- 상반기
    SELECT FLAVOR, SUM(TOTAL_ORDER) AS TOTAL_ORDER  -- 총 주문량
    FROM JULY GROUP BY FLAVOR) B
ON A.FLAVOR = B.FLAVOR
ORDER BY (A.TOTAL_ORDER + B.TOTAL_ORDER) DESC  -- 총 주문량 더한 값
LIMIT 3;  -- 상위 3개
