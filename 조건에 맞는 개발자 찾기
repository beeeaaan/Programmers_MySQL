SELECT ID, EMAIL, FIRST_NAME, LAST_NAME
FROM DEVELOPERS
// 2진수로 변환하여 확인
WHERE SKILL_CODE & (SELECT CODE FROM SKILLCODES WHERE NAME = 'Python')  // python 스킬
OR SKILL_CODE & (SELECT CODE FROM SKILLCODES WHERE NAME = 'C#')  // c# 스킬
ORDER BY ID;

SELECT 속성
FROM 테이블
WHERE 조건절
ORDER BY 속성;
