SELECT ID, EMAIL, FIRST_NAME, LAST_NAME
FROM DEVELOPER_INFOS
WHERE 'Python' IN (SKILL_1, SKILL_2, SKILL_3)  // OR 조건 사용
ORDER BY ID;

// SELECT 속성
// FROM 테이블
// WHERE 조건 IN (속성)  (IN : 특정 값 존재하는지 확인하기)
// ORDER BY 속성;
