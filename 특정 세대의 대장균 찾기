SELECT A.ID
FROM ECOLI_DATA A  -- 3세대 대장균
JOIN ECOLI_DATA B ON A.PARENT_ID = B.ID  -- 2세대 대장균(A의 부모)
JOIN ECOLI_DATA C ON B.PARENT_ID = C.ID  -- 1세대 대장균(B의 부모)
WHERE C.PARENT_ID IS NULL  -- 첫번째 세대 대장균 확인하기
ORDER BY A.ID;
