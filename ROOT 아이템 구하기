SELECT A.ITEM_ID, A.ITEM_NAME 
FROM ITEM_INFO A 
INNER JOIN ITEM_TREE B ON A.ITEM_ID = B.ITEM_ID
WHERE B.PARENT_ITEM_ID IS NULL;  // ROOT 

SELECT <열 목록>
FROM <테이블 별칭> A
INNER JOIN <테이블 별칭> B ON <조인될 조건>
WHERE <검색 조건>
